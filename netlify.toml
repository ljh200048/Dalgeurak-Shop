[build]
  command   = "npm run build"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA 라우팅: /classes, /notice 등 모든 경로를 index.html로 처리
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
